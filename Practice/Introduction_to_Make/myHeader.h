/* Modularizing the project:

Placing common material in header file, that can be included when required

Contains function declarations and macro definitions to be shared b/w source files
*/

int factorial(int n);
int add(int a, int b);
int power(int base, int exp);